# 第二周测试报告

## 1. 测试范围

本报告覆盖 PRD 第二周的桌面感知与控制模块：屏幕截图、多分辨率坐标转换、OCR 文本元素定位、边界框绘制，以及点击、输入、滚动、拖拽。测试分为自动单元测试、桌面功能验证和最终集成测试，三类结果不互相替代。

## 2. 测试环境

| 项目 | 配置 |
|---|---|
| 日期 | 2026-09-20 |
| Git 基线 | `b5a647a` |
| 操作系统 | Windows 11，AMD64 |
| Python | 3.10.21 |
| 屏幕 | 2560 × 1600，150% 缩放，单显示器 |
| GPU | NVIDIA GeForce RTX 4070 Laptop GPU |
| PyTorch / CUDA | 2.13.0+cu130 / 13.0 |
| EasyOCR | 1.7.2，`ch_sim` + `en`，CUDA |
| OpenCV / mss / PyAutoGUI | 5.0.0 / 10.2.0 / 0.9.54 |

macOS 和 Linux 没有可用实机，本报告不声称这两个平台已经通过桌面验证。

## 3. 自动单元测试

命令：

```powershell
python -m pytest -q
```

结果：`36 passed in 0.28s`。

| 模块 | 用例数 | 覆盖内容 | 结果 |
|---|---:|---|---|
| `test_capture.py` | 13 | BGR 截图、元数据、会话复用、分辨率与坐标转换、异常输入 | 通过 |
| `test_perception.py` | 9 | OCR 结果归一化、置信度过滤、空结果、坐标还原、边界框绘制 | 通过 |
| `test_control.py` | 14 | fail-safe、点击、ASCII 输入、滚动、拖拽、参数校验 | 通过 |

所有 OCR 和控制单测均使用假后端或内存数据；单测没有加载模型、移动鼠标或发送键盘输入。

## 4. 桌面功能验证

### 4.1 截图与坐标转换

- 使用同一 mss 会话连续截取 5 帧，全部成功；
- 截图区域：`(0, 0, 2560, 1600)`；
- BGR 图像形状：`(1600, 2560, 3)`；
- 缩放尺寸：1280 × 800；
- 中心点 `(1280, 800)` 经缩放及还原后保持不变；
- 边界框 `(640, 400, 1920, 1200)` 经缩放及还原后保持不变。

结果：通过。

### 4.2 OCR 与边界框

- 使用高对比度 PyQt5 测试窗口；
- 将窗口截图缩小到 50% 后运行 OCR，再把结果还原到原尺寸；
- 检测到 6 个文本元素；
- 英文数字目标 `7429` 和中文目标“屏幕文字识别”均识别成功；
- 人工检查带框裁剪图，绿色边界框与目标文字位置对应。

结果：通过。未设置固定命中率门槛，漏识别和误识别作为实际结果记录。

### 4.3 鼠标键盘控制

| 动作 | 判定依据 | 结果 |
|---|---|---|
| 点击 | 按钮状态更新 | 通过 |
| ASCII 输入 | 输入框内容为 `GUI Agent Control 123` | 通过 |
| 滚动 | 滚动条从 0 变为 2 | 通过 |
| 拖拽 | 滑块从 20 变为 98 | 通过 |

所有动作均只在受控测试窗口内执行，PyAutoGUI fail-safe 保持开启。

## 5. 最终集成测试

流程：测试窗口截图 -> 缩放 -> EasyOCR -> 边界框还原 -> OCR 目标中心点 -> PyAutoGUI 点击和输入 -> 窗口状态确认。

最终结果：

- 原窗口截图：1102 × 896；
- OCR 处理尺寸：826 × 672；
- OCR 定位按钮文本 `CLICK`，桌面坐标 `(834, 766)`；
- OCR 定位输入文本 `ASCII`，桌面坐标 `(951, 766)`；
- 点击后按钮状态更新；
- 输入框最终内容为 `Integrated GUI Agent 456`；
- 带框窗口图经人工检查，文字框位置正确。

结果：通过。

## 6. 失败案例

### F-01：整屏 OCR 产生目标假阳性

- 现象：首次 OCR 验证按 `7429` 搜索时，误匹配到背景编辑器中的源码 `if "7429"`；
- 原因：OCR 输入为整个桌面，测试窗口没有与背景应用隔离；
- 修复：根据 Qt 窗口逻辑坐标和 mss 物理分辨率裁剪测试窗口，随后只在窗口区域识别；
- 复测：英文目标和中文目标均在测试窗口内命中，边界框正确。

### F-02：集成目标文字过小

- 现象：50% 缩放时，按钮和输入框文字被识别为 `Lalget` 等错误内容，集成测试在执行动作前停止；
- 原因：按钮字体较小，输入框使用低对比度占位文字，缩小后不利于 OCR；
- 修复：改为高对比度大号 `CLICK` 和 `ASCII INPUT`，集成处理尺寸调整为原窗口的 75%；
- 复测：OCR 正确定位两个目标，点击和输入闭环通过。

### F-03：受限执行会话无法截图

- 现象：受限会话中 mss 调用 Windows `BitBlt` 失败；
- 原因：执行会话没有真实桌面访问权限，并非依赖或截图代码故障；
- 处理：桌面功能测试在获准的交互桌面会话中运行；
- 复测：真实桌面截图和最终环境验证通过。

## 7. 环境回归与限制

- `pip check`：无依赖冲突；
- `scripts/verify_environment.py --require-cuda`：通过；
- EasyOCR、OpenCV、mss、PyAutoGUI 和 CUDA 均正常；
- 本周输入模块只验证 ASCII；
- 多显示器负坐标、固定性能门槛、双 OCR 对比和非文字区域检测属于可选扩展，未执行；
- Windows 已实机验证，macOS/Linux 待实机验证。

## 8. 验收结论

PRD 第二周要求的截图、多分辨率适配、OCR 文本元素识别、坐标定位、边界框绘制，以及点击、输入、滚动、拖拽均已实现并通过对应单元测试和 Windows 桌面功能验证。感知到控制的集成闭环通过。
